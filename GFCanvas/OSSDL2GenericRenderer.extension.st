Extension { #name : #OSSDL2GenericRenderer }

{ #category : #'*GFCanvas' }
OSSDL2GenericRenderer >> readPixels: rect format: fmt pixels: pixels pitch: pitch [
	^ renderer readPixels: rect format: fmt pixels: pixels pitch: pitch

]
